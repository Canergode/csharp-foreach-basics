# Foreach Basics

This project demonstrates the foreach loop in C# for iterating through both string and integer arrays without needing an index variable.

## What I Learned
- How foreach simplifies array iteration
- How var automatically infers the element type
- How foreach works with both string and int arrays
- How foreach differs from for loop — no index needed
